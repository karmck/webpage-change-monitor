{
  "name": "webpage-change-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Monitor webpages for content changes.",
  "scripts": {
    "start": "node src/index.js",
    "start:once": "node src/index.js --once",
    "deploy": "npm run start:once && npx gh-pages -d public",
    "web-ui": "node src/web-ui.js"
  },

  "dependencies": {
    "jsdom": "^25.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
